# App server
APP_PORT=8080
APP_ORIGIN=http://localhost:8080

# Entra app settings (client-specific)
CLIENT_ID=YOUR_CLIENT_ID
TENANT_ID=YOUR_TENANT_ID
TENANT_SUBDOMAIN=YOUR_TENANT_SUBDOMAIN
ENTRA_AUTHORITY_HOST=ciamlogin.com

# Optional explicit authority override
# AUTHORITY=https://YOUR_TENANT_SUBDOMAIN.ciamlogin.com/YOUR_TENANT_ID

# Native Auth API target configuration
LOCAL_API_PATH=/api
CORS_PORT=3001
# Optional explicit target override (wins over TENANT_SUBDOMAIN/TENANT_ID)
# PROXY_TARGET=https://YOUR_TENANT_SUBDOMAIN.ciamlogin.com/YOUR_TENANT_ID

# Browser-facing API URL override (optional)
PUBLIC_BASE_API_URL=http://localhost:3001/api

# OAuth redirect
REDIRECT_URI=http://localhost:8080

# UI locale (examples: en, es)
LOCALE=en

# Default UI theme (azure-portal | enterprise-blue | fintech-slate)
THEME=azure-portal

# Comma-separated delegated scopes
LOGIN_SCOPES=openid,profile,email,UserAuthenticationMethod.Read,UserAuthMethod-Phone.ReadWrite

# cors_prod origin allowlist (comma-separated)
ALLOWED_ORIGINS=http://localhost:8080